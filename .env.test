# Fake, non-secret values for `npm test` (unit tests only — no real DB/Discord
# app is ever touched). Safe to commit. See scripts/run-tests.ts + package.json.
DATABASE_URL="postgresql://test:test@localhost:5432/testdb"
DISCORD_APP_ID="test-app-id"
# Public half of a throwaway keypair generated for verify.test.ts; the paired
# private key lives only in the test fixture, never here.
DISCORD_PUBLIC_KEY="4ac5d33ed5a59abf72cc3fdee52c8762145ad469729a100b9853ea5a919690de"
DISCORD_BOT_TOKEN="test-bot-token"
AUTH_SECRET="test-auth-secret-not-real"
NODE_ENV="test"
